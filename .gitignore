# macOS cruft
.DS_Store

# Python
__pycache__/
*.pyc
.venv/
venv/

# Environment / secrets
.env
*.env

# Ignore everything in config (so we can ignore real config files)
config/*
# Allow template/example config to be tracked (so we have an anonymous version to work with)
!config/README.md
!config/project_pay_rate_overrides_template.csv

# Data & logs (real company data stays local only)
data/
logs/

# Databases / local analytics stores
*.duckdb
*.sqlite
*.db
